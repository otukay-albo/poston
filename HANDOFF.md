# Poston 引き継ぎ資料

最終更新: 2026-09-07 / 作成者: 大塚

---

## 1. 一行でいうと今どこ

**TikTokアプリ審査の4回目を提出し、結果待ち。**（2026-09-07 提出）
過去3回の指摘はすべて対応済み。デモ動画も全権限を実演したものに撮り直して提出した。
アプリは**本番環境に復帰済み**なので、通常運用してよい状態。

---

## 2. 環境の状態：本番（復帰済み）

デモ撮影のため一時的にサンドボックスへ切り替えていたが、**提出後に本番へ戻して検証済み**。

| 項目 | 現在の値 |
|---|---|
| `NEXT_PUBLIC_TIKTOK_CLIENT_KEY` | `awnu4vv0a4r9e33s`（本番）※Config型 |
| `TIKTOK_CLIENT_SECRET` | 本番のsecret ※Secret型 |
| 要求スコープ | `user.info.basic, user.info.stats, video.list, video.publish, video.upload` |

※ `NEXT_PUBLIC_TIKTOK_SCOPES` は**設定していない**（コードの既定値で正しく動く）。もし誰かが追加していたら削除する。

### もう一度サンドボックスで撮影が必要になったら
上記2つをサンドボックスの値（`sbaw13956eb823vg88` ＋ サンドボックスsecret）に変えて Redeploy。
サンドボックスは Products / Scopes / Redirect URI / ターゲットユーザーが**本番と別管理**なので注意。
ターゲットユーザーには **sana**（投稿実績あり・撮影に使用）を追加済み。

現在どちらで動いているかの確認コマンドは本書「4. 撮影後」の項を参照。

---

## 3. 審査の経緯（何を指摘され、どう直したか）

| 回 | 指摘 | 原因 | 対応 |
|---|---|---|---|
| 1 | Privacy Policy / Terms が不十分 | 内容が形式的だった | 両ページを実態に沿って全面拡充（運営者=株式会社CrescenDo） |
| 2 | App icon does not match brand | ブラウザタブのアイコンがNext.jsの初期画像のままで、サイトのロゴと別物だった | 統一アイコンを作成し、サイト・favicon・TikTok登録の3箇所を同一に |
| 3 | **Scopes mismatch** | 申請権限に含まれる「下書き保存(video.upload)」を実演していなかった。説明欄に「video.uploadは使っていない」と書いていたのが直接の原因。加えて「サンドボックスで実演せよ」と指示 | 下書き送信機能を実装＋公式UXガイドラインの未実装項目を全て実装＋サンドボックスで撮り直し＋説明欄を書き換え |
| 4 | **結果待ち**（2026-09-07 提出） | — | デモ動画3本（Video1=ログイン/分析、Video2=公開投稿、Video3=下書き送信）＋説明欄を全面書き換え＋アイコンをサイトと同一に |

### 3回目の指摘に対して実装したもの（すべて完了）

1. **下書き送信機能**（`video.upload` の実演用）
   - 投稿タイミングが「今すぐ投稿 / 予約する / 下書きに送る」の3択に
   - TikTokアプリ側で音楽・エフェクトを付けて本人が投稿する導線
2. **商用コンテンツ開示**（公式必須項目だが未実装だった＝落ちる要因）
   - 既定オフのトグル →「あなたのブランド」「ブランドコンテンツ」
   - 未選択なら投稿ボタン無効化／ラベル表示／ブランドコンテンツは非公開投稿を禁止
3. **インタラクション設定を「〜を許可する」形式に**（初期値は全て未チェック）
4. **「処理に数分かかる場合があります」の案内**

---

## 4. 動作確認の状況

| 機能 | 状態 |
|---|---|
| ログイン | ✅ 成功（サンドボックス・本番とも） |
| 下書き送信（`video.upload`） | ✅ 成功（64MB未満で確認・スマホの受信箱に到着） |
| 公開投稿（`video.publish`） | ✅ 成功（64MB未満で確認） |
| 分析・集計・期間絞り込み | ✅ 動作（sanaで確認） |
| 大容量動画（101.7MB）の送信 | ❌ 失敗中（下記5参照） |

### 投稿テストの前提条件
- 投稿先アカウントを**TikTokアプリで「非公開アカウント」に設定**しておく
  （設定とプライバシー → プライバシー → 非公開アカウント ON）
- 審査前は公開範囲「自分のみ」でしか投稿できない（`unaudited_client_can_only_post_to_private_accounts`）

---

## 5. 未解決の課題

### (A) 大容量動画（64MB超）のアップロードが失敗する
- 症状：101.7MBの動画で `The chunk size is invalid [invalid_params]`
- 調査済みのこと：
  - サーバーは公式ルール通りの値を送っている（検証済み）
  - 64MB以下（小さい動画）は成功する
  - 48.5MB×2分割 → 失敗 / 10MB分割に変更 → **未検証のまま中断**
- 実装：`app/lib/chunk.ts`（分割ロジック。1MB〜4GBでルール適合をテスト済み）
- 影響：**デモ撮影の障害にはならない**（小さい動画で撮ればよい）。実運用でも
  TikTok投稿は10〜30MB程度が普通なので優先度は高くない
- 次の一手：10MB分割で再テスト。ダメなら分割せず「64MB超は非対応」と割り切る案もある

### (B) `yuki_beauty` の蓄積データが多すぎて分析に全部反映されない
- `video_stats` に **558,981行**（GASが長期間くり返し収集した結果）
- APIが**先頭1000行しか返さない**（データベースの既定上限）ため、分析結果が不正確
- `sana`（124行）・`Seo-yeon`（114行）は影響なし
- 対応案：古いスナップショットを間引く（動画ごとに1日1件残す等）／
  API側で「動画ごとの最新のみ」を取得する形に変える

### (C) 自動データ収集が確実に動いていない可能性
- `CRON_SECRET` が未設定のため、Vercelの毎日1回の自動収集が動いていないおそれ
- 当面はアナリティクス画面の「🔄 更新」ボタンで手動収集できる
- 設定すれば毎日自動で溜まる（頻度を上げたい場合は cron-job.org 等で30分間隔も無料）

---

## 6. 保留中／判断待ちの項目

| 項目 | 状態 |
|---|---|
| TikTok Studio CSV取り込み（`/import`） | 実装済みだがナビ非表示で保留。上司判断待ち |
| 有料API（Phyllo等・月$199〜）の契約 | 保存数・視聴時間は取得できない見込みと報告済み。問い合わせ文面は作成済み |
| 自動データ収集の高頻度化 | 現在1日1回（Vercel無料枠の上限）。cron-job.org等で30分間隔も無料で可能 |
| `CRON_SECRET` の設定 | 未設定の可能性あり。設定すると毎日の自動収集が確実に動く |

---

## 7. 主要な実装メモ

### データの流れ
- **投稿**：ブラウザ →（初期化のみVercel経由）→ 動画本体はブラウザから直接TikTokへPUT
  ※Vercelのリクエスト上限4.5MBを回避するため
- **分析**：Vercel Cron（1日1回）→ `tiktok_accounts`の全アカウント → TikTok API → Supabase `video_stats` → 画面
  ※以前はGAS依存だったが、Poston自身が収集する方式に置き換え済み

### 主要ファイル
| ファイル | 役割 |
|---|---|
| `app/lib/tiktokToken.ts` | トークン管理・複数アカウント・チーム同期 |
| `app/lib/chunk.ts` | アップロードの分割計算 |
| `app/api/post/route.ts` | 公開投稿の初期化 |
| `app/api/upload-draft/route.ts` | 下書き送信の初期化 |
| `app/api/collect-stats/route.ts` | 自動データ収集（Cron／手動更新ボタン） |
| `app/post/page.tsx` | 投稿画面（審査要件のUXはすべてここ） |

### 環境変数（Vercel）
```
NEXT_PUBLIC_TIKTOK_CLIENT_KEY   ← 現在サンドボックス。要復帰
TIKTOK_CLIENT_SECRET            ← 現在サンドボックス。要復帰
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
POSTON_TEAM_KEY                 ← チーム同期の合言葉
CRON_SECRET                     ← 未設定の可能性
GAS_API_KEY                     ← 旧収集用。現在は不要
```

### 取得できないデータ（調査済み・結論）
保存数／平均視聴時間／フル視聴率／視聴者の性別・年齢・地域／国別リーチ／
トラフィックソース／写真投稿のインサイト
→ **TikTok APIが提供しておらず、Studioの書き出しにも含まれない**（実物で検証済み）

---

## 8. 次にやること

### 審査結果が出たら
- **承認された場合**：`app/post/page.tsx` の `審査済み = false` を `true` にすると
  公開範囲を「自分のみ」以外も選べるようになる。次に Content Posting API（②）を再申請 →
  自動投稿（予約の実処理をサーバー側で実行）の実装へ
- **また落ちた場合**：指摘文をそのまま持ってきて原因を切り分ける。
  過去3回の指摘と対応は本書3章にまとめてある

### 審査待ちの間にできること（優先度順）
1. **大容量動画（64MB超）の再検証** — 10MB分割に変更済みだが未検証（5-A）
2. **`CRON_SECRET` の設定** — 自動データ収集を確実に動かす（5-C）
3. **`yuki_beauty` の古いデータ整理** — 分析が先頭1000行しか反映されない問題（5-B）
4. **CSV取り込み／有料APIの判断** — 上司判断待ち（6章）

### 撮影に使ったファイル（デスクトップに保管）
`Video1.mp4`（ログイン・分析）/ `Video2.mp4`（公開投稿）/ `Video3.mp4`（下書き送信）/
`Poston_icon_1024.png`（サイトのfaviconと画素単位で一致・提出済みアイコン）
→ 再提出が必要になった場合に部分的に使い回せるので消さないこと

---

## 9. 作業のお願い（ユーザーの好み）

- 日報は指定フォーマット（作業内容／次にやること／出来たこと／改善すること）で、
  **非エンジニアにも伝わる言葉**で書く
- 「中間報告して」と言われたら、Slack用のまとめを出す
- 出力はSlackのコードブロック形式が基本
